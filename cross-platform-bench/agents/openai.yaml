interface:
  display_name: "Cross Platform Bench"
  short_description: "Create sbench cross-platform benchmarks"
  default_prompt: "Use $cross-platform-bench to create an sbench benchmark with JSONL output and optional scheduler control."
